#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a = 5, b = 3;
    double result = (a * a) + (b * b); // Correct formula
    cout << "Result: " << result << endl;
    return 0;
}